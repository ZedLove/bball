root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown files: preserve intentional trailing spaces (two spaces = hard line break)
[*.md]
trim_trailing_whitespace = false
